import { Post } from './post';

describe('Post', () => {
  it('should create an instance', () => {
    // expect(new Post(new Date(),"This is desc",1,[],"This is title",10,{})).toBeTruthy();
  });
});
